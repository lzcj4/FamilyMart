<UserControl x:Class="FamilyMartUI.UC.UCReportParser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBox Margin="5" Name="txtReport" AcceptsReturn="True" AcceptsTab="True" />
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" >
            <Button Content="解析" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Button_Click" />
            <Button Content="文件夹解析" Margin="100,0,0,0"  VerticalAlignment="Center" Width="100" Click="Button_Click_1" />
        </StackPanel>
    </Grid>
</UserControl>
